# Notes
```
=> FIND STUFF
// "" necessary if spaces in string
// ALL is optional
=> CHANGE "YOUR NAME" "TEST NAME" ALL
// Change scroll number for f8
Scroll => 3

All params except first one must be on column 16 (first one only separatated by space)

// NOTIFY=&SYSUID
if RC=0000 no errors

// EXEC is also called STEPNAME

// submit job
=> SUB

// Go to held queue
=> =sd.h

CONDition CODE, CC, Return Code, RC all mean the same thing.
```